targets:
  {{Module}}:
    type: framework
    platform: iOS
    sources:
      - path: Sources
        name: {{Module}}
    configFiles:
      Debug: xcconfigs/{{Module}}-Debug.xcconfig
      Release: xcconfigs/{{Module}}-Release.xcconfig
