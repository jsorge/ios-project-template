targets:
  {{Module}}:
    type: application
    platform: macOS
    sources:
      - Sources
      - Resources
    configFiles:
      Debug: xcconfigs/{{Module}}-Debug.xcconfig
      Release: xcconfigs/{{Module}}-Release.xcconfig
